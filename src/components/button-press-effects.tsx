"use client";

/**
 * ⚠️ RECOVERED-STUB (2026-06-13)
 *
 * The original `src/components/button-press-effects.tsx` was NOT present in any
 * Claude Code or Codex transcript on this machine and could not be
 * reconstructed. It was a global client component (rendered once in
 * `layout.tsx`) that added button-press micro-interactions.
 *
 * This stub is a no-op so the app builds and runs. Restore the original
 * component to bring back the press effect.
 */
export function ButtonPressEffects() {
  return null;
}
