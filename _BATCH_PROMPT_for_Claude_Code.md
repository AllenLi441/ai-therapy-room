# 给 Claude Code 的批次指令(2026-06-14)

> 这一批分**两条流**:① 非危机(可正常验证→合并上生产);② v2 危机重构(分支专用、出草案、**待心理老师评审签字后才上线**)。
> 老规矩:匹配作者 `AllenLi441 <245363491+AllenLi441@users.noreply.github.com>`、每项可验证证据、tsc/build/test 绿。**这一批可以攒着做,别一颗一颗挤**;非危机几项可放一个分支一次交付。

---

## 流 ①:非危机(做完→Cowork 验→合并上生产)

### 1. 进入同意/免责门
- 进入聊天前显示一屏**免责 + 同意**:点「进入」即代表"已阅读并同意"。文案沿用现有边界语:**这是 AI 陪伴,不是医生/持证咨询师;危机时请联系现实中的真人与急救;对话只存在本地**。
- 记住同意状态(localStorage,沿用 `quiet-room-disclaimer-*`),避免每次刷新都拦;「一键彻底删除」后重新征得同意。免责文案在「关于安屿」里随时可查。

### 2. (A) 深度/快速 = 纯切模型(不是改文案)
- 把深度/快速开关改成发 `model`:**深度 → `deepseek-v4-pro`,快速 → `deepseek-v4-flash`**(后端 `route.ts` 已有 `body.model` → `resolveDeepSeekModel`,基建在 `model-options`)。
- **撤掉** pace-改回复结构/长度的逻辑(`6eeb6fb` 把 pace 接进 prompt + `a88318c` 输出格式按 pace 分支);**`p3c-pace` 不要合并**。
- **保留**安屿声音(已批准,独立于此);只是开关语义从"回复形状"改成"模型"。更新相关单测。

### 3. (B) 真实进度指示(选③:真实字数增长条)
- 等首字阶段保持现有"思考中 · 真实秒数";开始流式后,加一个**随真实到达字符增长**的细进度条。**禁止**假倒计时/与生成无关的填充。出错仍如实显示错误态。(此项可调,目前定③。)

---

## 流 ②:v2 危险识别重构(分支专用 · 不上生产 · 出草案待评审)

按 `_SAFETY_v2_现状审计与解耦方案.md`。**尽情用 subagents 把工程做深**,但严守边界。

### 可放开建(纯工程/结构,无需签字):
- **3a 确定性安全地板**:抽成 LLM 无关的独立组件;热线/资源做成**单一真值源**(如 `crisis-resources.ts`),消除 `safety.ts` 与 `overlays.tsx` 两份漂移;用**显式状态**替代"匹配模板原文"判危机持续态。
- **3b 多源分级 + 检测/回应解耦**:词典 + Kimi + 量表 + 轨迹 → 一个分级信号(词典降为输入之一);算出分级 → **地板按阈值必出** + 回应交给模型生成(替掉复读模板)。
- **KB/RAG 检索骨架**(`knowledge.ts` 现为空桩)。
- **影子运行**(decision-log)对比新旧检测的误报/漏报;完整单测 + 影子数据。

### 必须出"评审就绪草案"、不得擅自定稿/上线:
- 危机文案、分级阈值、隐式 fail-open 政策、KB 临床内容、未成年识别(12355)措辞与触发——**全部做成带标记的草案**,集中放一处(如 `_SAFETY_v2_DRAFTS_待评审.md`),供心理老师过目。

### 未成年 12355 加性修复(加性、低下行):
- 在所有危机/自杀回应出口调 `hasMinorContextCue(当前消息)`,命中则**追加** `createMinorSupportLine(language)`(集中为 `withMinorSupport`)。**只增不删**成人热线。
- 12355 的措辞 + 触发判定一并进上面的草案,给心理老师确认;因其加性、下行≈0,确认措辞后可较快上线。

---

## 硬约束(全批)
- 流① 非危机:正常流程(Cowork preview/prod 验 → 合并)。
- 流② v2:**分支专用、不部署生产**;**任何危机文案/阈值/fail-open/未成年措辞的最终值 = 先心理老师签字、Cowork 复测、用户点头,才灰度上线。**
- 生产现冻结在最新 main;不碰已上线的危机行为直到 v2 走完评审。
- 做不到/有取舍直说,不造假。

---

## 交付顺序建议
1. 先交**流①**三项(同意门 + 切模型 + 进度条)→ Cowork 验 → 合并。
2. 并行推进**流② 工程骨架 + 草案**;草案产出后交给 Cowork,Cowork 整理成"心理老师评审包"给用户。
3. 评审签字回来后,再落 v2 危机文案/阈值定稿 + 未成年修复上线。
